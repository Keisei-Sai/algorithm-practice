# Algorithm Practice (Python)

This repository contains clean Python implementations for interview preparation.

## Two Sum
- Approach: Hash map (value -> index)
- Time Complexity: O(n) average
- Space Complexity: O(n)

File: `two_sum.py`

## Greedy / One-pass

- Best Time to Buy and Sell Stock — One-pass greedy, O(n) time / O(1) space (`best_time_buy_sell_stock.py`)

## Hash / Set

- Contains Duplicate — Hash set, O(n) time / O(n) space (`contains_duplicate.py`)
